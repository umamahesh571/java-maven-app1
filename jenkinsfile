pipeline {
    agent any

    stages {
        stage('Checkout Code') {
            steps {
                git branch: 'main', url: 'https://github.com/umamahesh571/java-maven-app1.git'
            }
        }

        stage('Build') {
            steps {
                script {
                    sh 'mvn clean package'
                }
            }
        }

        stage('Deploy') {
            steps {
                script {
                    sh '''
                    FROM openjdk:latest
                    COPY target/*.jar /app.jar
                    CMD ["java", "-jar", "/app.jar"]
                    
                    docker build -t my-java-app .
                    docker run -d -p 8081:8081 --name java-container my-java-app
                    '''
                }
            }
        }
    }
}
